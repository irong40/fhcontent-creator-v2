---
phase: 05-worker-workflows
plan: "05-04"
type: execute
wave: 5
depends_on:
  - "05-03"
  - "05-01"
files_modified:
  - vercel.json
autonomous: false
requirements:
  - CUT-03
  - CUT-04

must_haves:
  truths:
    - "WF-4 and WF-2 both run in parallel with their Vercel counterparts for at least 48 hours"
    - "Content published and assets initiated by n8n match Vercel cron behavior"
    - "daily-publish and daily-media cron entries removed from vercel.json only after human confirms parity"
    - "vercel.json deployed to Vercel after both crons removed"
    - "vercel.json crons array is empty after this plan completes"
  artifacts:
    - path: "vercel.json"
      provides: "Vercel config with all 3 crons removed (empty crons array)"
      contains: "\"crons\": []"
  key_links:
    - from: "vercel.json crons array"
      to: "Vercel daily-publish and daily-media crons"
      via: "Remove both entries after 48h parallel run confirms parity"
      pattern: "daily-publish|daily-media"
---

<objective>
Cut over the two remaining Vercel cron jobs (daily-media at 6AM and daily-publish hourly) to n8n WF-2 and WF-4. Run both pairs in parallel for 48 hours, verify output parity, then remove both cron entries from vercel.json in a single deployment. This checkpoint plan requires human sign-off before the destructive step.
</objective>

<context>
@D:/Projects/fhcontent-creator-v2/.planning/ROADMAP.md
@D:/Projects/fhcontent-creator-v2/.planning/STATE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Activate WF-4 and WF-2 and confirm Vercel crons still active</name>
  <files>n8n-workflows/CUTOVER-LOG.md</files>
  <action>
1. In n8n UI, activate both workflows:
   - "WF-4 Publish Pipeline" → toggle Active
   - "WF-2 Media Pipeline" → toggle Active

2. Confirm Vercel crons still present in vercel.json:
   - `daily-publish`: `0 * * * *` (hourly)
   - `daily-media`: `0 6 * * *` (6AM)

3. Verify idempotency: both n8n and Vercel will call the same Next.js routes simultaneously during parallel run. Confirm the routes are idempotent:
   - `/api/cron/daily-publish`: should not double-publish if called twice in same hour window
   - `/api/cron/daily-media`: should not initiate duplicate asset jobs if called twice at 6AM

4. If routes are NOT idempotent, a staggering strategy is needed:
   - Option A: Offset n8n trigger by 2 minutes (e.g., `2 6 * * *` for WF-2, `2 * * * *` for WF-4) so Vercel fires first and n8n is a follow-up
   - Option B: Add a `parallel_run=true` flag to n8n calls so Next.js routes skip processing if Vercel already ran

5. Append to `n8n-workflows/CUTOVER-LOG.md`:
```markdown
## Worker Cron Parallel Run Start
- Date: [YYYY-MM-DD HH:MM UTC]
- WF-4 activated: YES
- WF-2 activated: YES
- Vercel daily-publish cron: ACTIVE
- Vercel daily-media cron: ACTIVE
- Idempotency strategy: [describe]
- Expected parallel run end: 48 hours after start
```
  </action>
  <verify>Both WF-2 and WF-4 show Active in n8n. Vercel dashboard shows 2 active crons. CUTOVER-LOG.md updated.</verify>
  <done>Parallel run started for both worker workflows.</done>
</task>

<task type="auto">
  <name>Task 2: Monitor 48-hour parallel run</name>
  <files>n8n-workflows/CUTOVER-LOG.md</files>
  <action>
After 24 hours, run these Supabase queries:

```sql
-- WF-4 publish run count (expected: ~24 per day)
SELECT COUNT(*) as wf4_runs
FROM pipeline_runs
WHERE workflow_id = 'WF-4'
  AND started_at > NOW() - INTERVAL '24 hours';

-- WF-2 media run count (expected: 1 per day)
SELECT COUNT(*) as wf2_runs
FROM pipeline_runs
WHERE workflow_id = 'WF-2'
  AND started_at > NOW() - INTERVAL '24 hours';

-- Error check for both
SELECT workflow_id, COUNT(*) as errors
FROM pipeline_errors
WHERE workflow_id IN ('WF-2', 'WF-4')
  AND created_at > NOW() - INTERVAL '24 hours'
GROUP BY workflow_id;

-- DLQ check
SELECT COUNT(*) as dlq_items
FROM pipeline_dlq
WHERE created_at > NOW() - INTERVAL '24 hours';

-- Published content pieces
SELECT status, COUNT(*)
FROM content_pieces
WHERE updated_at > NOW() - INTERVAL '24 hours'
GROUP BY status;
```

Record results at 24h and 48h in CUTOVER-LOG.md.

Also check n8n execution history for both WF-2 and WF-4 — confirm all executions show "succeeded" status.
  </action>
  <verify>CUTOVER-LOG.md updated with 24h and 48h query results.</verify>
  <done>48-hour monitoring data recorded.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Human verification — confirm WF-4 and WF-2 parity</name>
  <files>vercel.json</files>
  <action>
STOP. Human review required before removing Vercel cron jobs.

Review checklist:

### WF-4 (Publish Pipeline) — Hourly
- [ ] WF-4 run count matches expected hourly cadence (>=40 runs in 48h, out of expected 48)
- [ ] Published content pieces count looks correct
- [ ] DLQ items <= 2 (minor failures acceptable)
- [ ] No duplicate publishes detected
- [ ] n8n-publish-callback Edge Function logs show valid HMAC signatures

### WF-2 (Media Pipeline) — 6AM Daily
- [ ] WF-2 ran on both days at ~6AM
- [ ] All 6 asset branches initiated (check n8n-asset-callback Edge Function logs)
- [ ] Asset job IDs stored in Supabase content_pieces/visual_assets/audio_assets tables
- [ ] No duplicate asset initiations
- [ ] WF-5 successfully polling the initiated jobs (check WF-5 logs)

### Overall
- [ ] pipeline_errors count is at or below baseline
- [ ] No data integrity issues in content_pieces or related tables
- [ ] Lock management working (no stale locks in workflow_locks)

If ALL checks pass: confirm "Worker cron cutover approved" to proceed to Task 4.
If ANY check fails: describe the issue and do NOT proceed. Fix and extend parallel run by 24h.
  </action>
  <verify>Human confirms all checklist items pass in writing.</verify>
  <done>Human has explicitly approved cutover for both WF-2 and WF-4.</done>
</task>

<task type="auto">
  <name>Task 4: Remove daily-publish and daily-media from vercel.json</name>
  <files>vercel.json</files>
  <action>
After human approval:

1. Read `D:/Projects/fhcontent-creator-v2/vercel.json`.
2. Remove both remaining cron entries:
   - `{ "path": "/api/cron/daily-publish", "schedule": "0 * * * *" }`
   - `{ "path": "/api/cron/daily-media", "schedule": "0 6 * * *" }`

3. The resulting crons array should be empty:
```json
"crons": []
```
   Or remove the crons key entirely if Vercel supports an empty array.

4. Deploy to Vercel: `vercel --prod` from `D:/Projects/fhcontent-creator-v2`.

5. Confirm in Vercel dashboard: Cron Jobs section shows 0 active crons.

6. Update CUTOVER-LOG.md:
```markdown
## Worker Cron Cutover Completed
- Date: [YYYY-MM-DD HH:MM UTC]
- daily-publish removed from vercel.json: YES
- daily-media removed from vercel.json: YES
- Deployed to Vercel: YES
- Vercel cron dashboard: 0 active crons
- n8n WF-2 and WF-4 are now sole owners of media generation and publishing
```
  </action>
  <verify>
- `grep -E "daily-publish|daily-media" vercel.json` returns no matches.
- Vercel dashboard shows 0 cron jobs.
- WF-2 fires at next 6AM without error.
- WF-4 fires at next hour boundary without error.
  </verify>
  <done>All Vercel crons removed. n8n is sole scheduler for all 3 workflows.</done>
</task>
</tasks>

<verification>
1. vercel.json crons array is empty or contains 0 entries.
2. Vercel dashboard shows 0 active cron jobs.
3. WF-2 runs at 6AM on the first day post-cutover.
4. WF-4 runs every hour on the first day post-cutover.
5. CUTOVER-LOG.md has timestamps for all parallel run milestones and cutover completion.
6. pipeline_runs has WF-2 and WF-4 rows with no gaps.
</verification>

<success_criteria>
- All 3 Vercel cron jobs replaced by n8n (check-status done in Phase 4, daily-publish and daily-media done here).
- Zero missed executions in 24 hours after final cutover.
- Human explicitly confirmed parity before each cron removal.
- vercel.json and CUTOVER-LOG.md both updated and committed.
</success_criteria>

<output>D:/Projects/fhcontent-creator-v2/n8n-workflows/CUTOVER-LOG.md</output>
